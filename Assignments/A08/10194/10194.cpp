#include <iostream>

using namespace std;

int main()
{
    char tournament; // store the tournament name
    string teamName; // store the name of the team
    int gamesCount;  // to store games played
    int pointsEarned;

    int N;
    string tournament_description;
  

    // [a]) T eam_name [b]p, [c]g ([d]-[e]-[f ]), [g]gd ([h]-[i])
    // • [a] = team rank
    // • [b] = total points earned
    // • [c] = games played
    // • [d] = wins
    // • [e] = ties
    // • [f ] = losses
    // • [g] = goal difference
    // • [h] = goals scored
    // • [i] = goals against
    // Example:
    // 1) Brazil 6p, 3g (2-0-1), 3gd (6-3)

    return 0;
}